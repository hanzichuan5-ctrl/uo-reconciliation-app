# 月度工作台范围

当前入口为“打开月度工作台.command”，主程序为 `monthly/`，采用Python、SQLite、FastAPI和本地网页。

- 收入任务从实际发票出发；系统记录仅用于核对对应收入，不能反向扫描全系统生成无票任务。开票月份、收入月份分别保存。
- 回款只填写用户指定底稿的“回款”Sheet，公司、金额、到账日期及必要序号、小计、该页格式。其他Sheet数值、公式、缓存、格式、透视表及对象原样保留，不做账龄、归属、发票匹配或核销分析。
- 只生成副本；若用户明确要求写原文件，先备份，再验证精确文件名和内容版本。
- 不读取、不引用、不依赖 `1.代理游戏联运渠道回款情况(20260909）.xlsx` 或其后续业务查看版本，也不通过旧规则、旧输出、旧网页数据间接沿用其结论。
- 腾讯FPP纳入决定按具体月份和交易保存，不能把一次确认扩为以后自动纳入。
- 月份候选、确实缺少资料、金额差异应分别说明；候选不可假装已确认。鸿蒙暂不检查收入同步状态，但开票对应和游戏拆分的未知信息须保留。
- 金额按整数分计算，票号按文本保存。Excel结果需要逐笔回读检查，原底稿其他包内容需要保留性检查。
- 新原始文件、生成Excel、数据库及运行环境不提交Git。实际系统操作状态以新UO导出回读为准，本地勾选只是用户确认。
- 页面采用深绿色导航、暖色底和高对比文字，保持表头与交替行易读。
